const express = require("express");
const multer = require("multer");
const fs = require("fs");
const os = require("os");
const path = require("path");
const https = require("https");

const app = express();
const PORT = process.env.PORT || 10000;
const REPO = process.env.REPO_FULL_NAME || "nusratbytxrs/release-hosting";
const SITE_NAME = process.env.SITE_NAME || "Release Hosting";
const GITHUB_TOKEN = process.env.GITHUB_TOKEN || "";
const ADMIN_KEY = process.env.ADMIN_KEY || "";
const MAX_UPLOAD_MB = Math.max(1, Number(process.env.MAX_UPLOAD_MB || 100));
const tempDir = fs.mkdtempSync(path.join(os.tmpdir(), "release-hosting-"));
const upload = multer({
  storage: multer.diskStorage({
    destination: (_req, _file, cb) => cb(null, tempDir),
    filename: (_req, file, cb) => cb(null, `${Date.now()}-${Math.random().toString(36).slice(2)}-${path.basename(file.originalname)}`)
  }),
  limits: { fileSize: MAX_UPLOAD_MB * 1024 * 1024 }
});
app.use(express.urlencoded({ extended: true }));
app.use(express.json({ limit: "2mb" }));

const esc = value => String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;");
const safeName = value => { const name = path.basename(String(value || "")).replace(/[\\/]/g, "-").trim(); return name && name !== "." && name !== ".." ? name : ""; };
const requestKey = req => req.headers["x-admin-key"] || req.query.key || req.body?.key || "";
function requireAdmin(req, res, next) { if (!ADMIN_KEY) return res.status(500).json({ ok: false, error: "ADMIN_KEY is not configured." }); if (requestKey(req) !== ADMIN_KEY) return res.status(401).json({ ok: false, error: "Invalid admin key." }); next(); }

async function githubApi(apiPath, options = {}) {
  if (!GITHUB_TOKEN) throw new Error("GITHUB_TOKEN is not configured.");
  const response = await fetch(`https://api.github.com${apiPath}`, { ...options, headers: { Accept: "application/vnd.github+json", Authorization: `Bearer ${GITHUB_TOKEN}`, "X-GitHub-Api-Version": "2022-11-28", ...(options.body ? { "Content-Type": "application/json" } : {}), ...(options.headers || {}) } });
  const text = await response.text(); let data;
  try { data = text ? JSON.parse(text) : {}; } catch { data = { raw: text }; }
  if (!response.ok) { const error = new Error(data.message || `GitHub API returned ${response.status}`); error.status = response.status; error.github = data; throw error; }
  return data;
}
const getReleases = () => githubApi(`/repos/${REPO}/releases?per_page=100`);
const getReleaseByTag = tag => githubApi(`/repos/${REPO}/releases/tags/${encodeURIComponent(tag)}`);

function uploadAsset(releaseId, filename, type, filePath, size) {
  return new Promise((resolve, reject) => {
    const req = https.request({ hostname: "uploads.github.com", method: "POST", path: `/repos/${REPO}/releases/${encodeURIComponent(releaseId)}/assets?name=${encodeURIComponent(filename)}`, headers: { Accept: "application/vnd.github+json", Authorization: `Bearer ${GITHUB_TOKEN}`, "X-GitHub-Api-Version": "2022-11-28", "Content-Type": type || "application/octet-stream", "Content-Length": String(size), Connection: "close" }, timeout: 20 * 60 * 1000 }, response => {
      const chunks = []; response.on("data", chunk => chunks.push(chunk)); response.on("end", () => { const text = Buffer.concat(chunks).toString("utf8"); let data; try { data = text ? JSON.parse(text) : {}; } catch { data = { raw: text }; } resolve({ status: response.statusCode || 500, ok: response.statusCode >= 200 && response.statusCode < 300, data }); });
    });
    req.on("timeout", () => req.destroy(new Error("GitHub upload timed out."))); req.on("error", reject);
    const stream = fs.createReadStream(filePath); stream.on("error", error => { req.destroy(); reject(error); }); stream.pipe(req);
  });
}

const CSS = `:root{--bg:#08090d;--surface:#11141c;--surface2:#191d28;--text:#f8fafc;--muted:#9aa5b5;--line:#293141;--accent:#7c5cff;--accent2:#23d5ab}body.light{--bg:#f4f7fb;--surface:#fff;--surface2:#edf1f7;--text:#111827;--muted:#5d6878;--line:#d8e0eb}*{box-sizing:border-box}body{margin:0;font-family:Inter,system-ui,Arial,sans-serif;background:var(--bg);color:var(--text)}a{color:inherit;text-decoration:none}.container{width:min(1120px,calc(100% - 32px));margin:auto}header{position:sticky;top:0;z-index:10;background:var(--bg);border-bottom:1px solid var(--line)}.nav{min-height:68px;display:flex;align-items:center;justify-content:space-between;gap:12px}.logo{font-weight:800;font-size:20px}.logo span{color:var(--accent)}.nav-links{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.btn{display:inline-flex;align-items:center;justify-content:center;padding:10px 14px;border-radius:12px;border:1px solid var(--line);background:var(--surface);color:var(--text);cursor:pointer}.btn:hover{border-color:var(--accent)}.btn-primary{border:0;background:linear-gradient(135deg,var(--accent),#4f9cff);color:#fff}.btn-danger{border-color:#ef4444;color:#ef4444}.btn:disabled{opacity:.6;cursor:wait}main{padding:28px 0 60px}.hero{padding:30px 0 20px}.hero h1{font-size:clamp(32px,6vw,58px);line-height:1.05;letter-spacing:-2px;margin:0 0 14px;background:linear-gradient(110deg,var(--text),var(--accent2));-webkit-background-clip:text;color:transparent}.hero p,.muted,.upload-meta,.rename-status{color:var(--muted)}.search{margin:20px 0 25px}.search input,.form-group input,.form-group select,.rename-input{width:100%;padding:13px;border-radius:11px;border:1px solid var(--line);background:var(--surface);color:var(--text);outline:none}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:18px}.card,.asset{background:linear-gradient(145deg,var(--surface),var(--surface2));border:1px solid var(--line);border-radius:18px;padding:22px;box-shadow:0 18px 55px #0003}.card h2{margin-top:0}.asset{margin-top:14px;position:relative}.asset-name{font-weight:700;word-break:break-word;padding-right:35px}.preview{margin-top:14px}.preview img{max-width:100%;max-height:450px;border-radius:14px}.preview video{width:100%;max-height:500px;border-radius:14px;background:#000}.notice{padding:16px;border-radius:14px;margin:16px 0;background:var(--surface2);border:1px solid var(--line)}.form-group{margin-bottom:20px}.form-group label{display:block;margin-bottom:8px;font-weight:700}.file-input{padding:20px!important}.selected-list{display:grid;gap:10px;margin:12px 0}.selected-file{display:grid;grid-template-columns:1fr 1fr auto;gap:8px;align-items:center;padding:10px;border:1px solid var(--line);border-radius:10px;background:var(--surface2)}.selected-file small{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.progress{height:14px;background:var(--line);border-radius:20px;overflow:hidden;margin-top:15px}.progress-bar{height:100%;width:0;background:linear-gradient(90deg,var(--accent),var(--accent2));transition:width .15s}.upload-meta{font-size:14px;margin-top:10px;line-height:1.5}.asset-menu{position:absolute;right:14px;top:14px}.dots{font-size:22px;width:38px;height:38px;padding:0}.menu-panel{display:none;position:absolute;right:0;top:44px;z-index:5;min-width:150px;padding:7px;border:1px solid var(--line);border-radius:12px;background:var(--surface);box-shadow:0 12px 35px #0008}.menu-panel.open{display:grid;gap:5px}.menu-panel button{padding:9px;text-align:left;border:0;border-radius:8px;background:transparent;color:var(--text);cursor:pointer}.menu-panel button:hover{background:var(--surface2)}footer{padding:34px 0;border-top:1px solid var(--line);color:var(--muted);text-align:center}@media(max-width:650px){.container{width:calc(100% - 22px)}.nav{min-height:62px}.nav-links .btn{padding:8px 10px}.selected-file{grid-template-columns:1fr}.asset-menu{right:10px}}
`;
function page(title, body) { return `<!doctype html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)} - ${esc(SITE_NAME)}</title><style>${CSS}</style></head><body><header><div class="container nav"><a class="logo" href="/"><span>◆</span> ${esc(SITE_NAME)}</a><div class="nav-links"><a class="btn" href="/">Releases</a><a class="btn" href="/admin">Upload</a><button class="btn" id="themeBtn" type="button">☀</button></div></div></header><main><div class="container">${body}</div></main><footer><div class="container">${esc(SITE_NAME)} · Fast, simple release hosting</div></footer><script>const t=localStorage.getItem('release_theme');if(t==='light')document.body.classList.add('light');const b=document.getElementById('themeBtn');if(b){b.textContent=document.body.classList.contains('light')?'🌙':'☀';b.onclick=()=>{document.body.classList.toggle('light');localStorage.setItem('release_theme',document.body.classList.contains('light')?'light':'dark');b.textContent=document.body.classList.contains('light')?'🌙':'☀'}};</script></body></html>`; }
function preview(asset) { const n=asset.name||"",u=asset.browser_download_url||""; if(/\.(jpg|jpeg|png|gif|webp|bmp|svg)$/i.test(n))return `<div class="preview"><img src="${esc(u)}" alt="${esc(n)}" loading="lazy"></div>`; if(/\.(mp4|webm|ogg|mov|m4v)$/i.test(n))return `<div class="preview"><video controls preload="metadata"><source src="${esc(u)}">Your browser does not support this video.</video></div>`; return ""; }

app.get("/", async (_req,res)=>{try{const rs=await getReleases();const cards=rs.filter(r=>!r.draft).map(r=>`<div class="card"><h2><a href="/release/${encodeURIComponent(r.tag_name)}">${esc(r.name||r.tag_name)}</a></h2><p class="muted">Version: ${esc(r.tag_name)}</p><p>📦 ${r.assets?.length||0} files</p><a class="btn btn-primary" href="/release/${encodeURIComponent(r.tag_name)}">Explore release →</a></div>`).join("");res.send(page("Releases",`<section class="hero"><h1>Share your releases<br>with style.</h1><p>Browse releases, preview media, and download files in one place.</p></section><div class="search"><input id="search" type="search" placeholder="⌕ Search releases..." oninput="searchReleases()"></div>${cards?`<div class="grid" id="releaseGrid">${cards}</div>`:`<div class="notice">No releases available yet.</div>`}<script>function searchReleases(){const v=document.getElementById('search').value.toLowerCase();document.querySelectorAll('#releaseGrid .card').forEach(c=>c.style.display=c.innerText.toLowerCase().includes(v)?'':'none')}</script>`))}catch(e){res.status(500).send(page("Error",`<div class="notice">${esc(e.message)}</div>`))}});

app.get("/release/:tag",async(req,res)=>{try{const r=await getReleaseByTag(req.params.tag);const assets=(r.assets||[]).map(a=>`<div class="asset"><div class="asset-menu"><button class="btn dots" type="button" onclick="toggleMenu(this)">⋯</button><div class="menu-panel"><button type="button" onclick="renameAsset(${a.id},'${esc(a.name).replace(/'/g,"\\'")}')">Rename</button><button type="button" onclick="deleteAsset(${a.id},'${esc(a.name).replace(/'/g,"\\'")}')">Delete</button></div></div><div class="asset-name">${esc(a.name)}</div><p class="muted">${(a.size/1024/1024).toFixed(2)} MB</p>${preview(a)}<br><a class="btn btn-primary" href="${esc(a.browser_download_url)}">Download ↓</a></div>`).join("")||`<div class="notice">This release has no files.</div>`;const body=`<section class="hero"><h1>${esc(r.name||r.tag_name)}</h1><p class="muted">Version: ${esc(r.tag_name)}</p></section><div class="notice">Admin actions are available from the ⋯ menu on each file.</div><h2>Files</h2>${assets}<script>function admin(){let k=localStorage.getItem('release_admin_key')||prompt('Enter admin key');if(k)localStorage.setItem('release_admin_key',k);return k||''}function toggleMenu(b){document.querySelectorAll('.menu-panel.open').forEach(x=>x.classList.remove('open'));b.nextElementSibling.classList.toggle('open')}async function renameAsset(id,old){const k=admin(),n=prompt('New filename:',old);if(!k||!n)return;const r=await fetch('/api/assets/'+id,{method:'PATCH',headers:{'Content-Type':'application/json','x-admin-key':k},body:JSON.stringify({name:n})});const d=await r.json();if(!r.ok||!d.ok){alert(d.error||'Rename failed');return}location.reload()}async function deleteAsset(id,name){const k=admin();if(!k||!confirm('Delete '+name+' permanently?'))return;const r=await fetch('/api/assets/'+id,{method:'DELETE',headers:{'x-admin-key':k}});const d=await r.json();if(!r.ok||!d.ok){alert(d.error||'Delete failed');return}location.reload()}</script>`;res.send(page(r.name||r.tag_name,body))}catch(e){res.status(e.status||500).send(page("Error",`<div class="notice">${esc(e.message)}</div>`))}});
app.get("/api/releases",async(_q,s)=>{try{const rs=await getReleases();s.json({ok:true,count:rs.length,releases:rs})}catch(e){s.status(e.status||500).json({ok:false,error:e.message})}});app.get("/api/admin/verify",requireAdmin,(_q,s)=>s.json({ok:true}));

app.get("/admin",async(_q,s)=>{try{const rs=await getReleases();const opts=rs.filter(r=>!r.draft).map(r=>`<option value="${esc(r.id)}">${esc(r.name||r.tag_name)} (${esc(r.tag_name)})</option>`).join("");const body=`<section class="hero"><h1>Upload files</h1><p>Select multiple files and add them to a release.</p></section><div class="card"><div id="keyBox" class="form-group"><label>Admin key</label><input id="adminKey" type="password" placeholder="Enter your admin key"></div><div class="form-group"><label>Destination release</label><select id="releaseId">${opts}</select></div><div class="form-group"><label>Select files <span class="muted">(each up to ${MAX_UPLOAD_MB} MB)</span></label><input id="file" class="file-input" type="file" multiple><div id="selected" class="selected-list"></div></div><button class="btn btn-primary" id="uploadBtn" type="button">Upload selected files ↑</button><div id="status"></div></div><script>const kb=document.getElementById('keyBox'),ki=document.getElementById('adminKey'),fi=document.getElementById('file'),sel=document.getElementById('selected'),out=document.getElementById('status'),btn=document.getElementById('uploadBtn'),saved=localStorage.getItem('release_admin_key'),files=[];function e(v){return String(v||'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;')}if(saved){ki.value=saved;kb.style.display='none'}function render(){sel.innerHTML=files.map((x,i)=>'<div class="selected-file"><small>'+e(x.name)+'</small><span class="muted">'+(x.file.size/1024/1024).toFixed(2)+' MB</span><button class="btn" data-i="'+i+'" type="button">Remove</button></div>').join('');sel.querySelectorAll('button').forEach(b=>b.onclick=()=>{files.splice(Number(b.dataset.i),1);render()})}fi.onchange=()=>{files.length=0;Array.from(fi.files).forEach(file=>files.push({file,name:file.name}));render()};function uploadOne(x,i,total,k,tryNo=0){return new Promise(resolve=>{const f=new FormData(),q=new XMLHttpRequest();f.append('release_id',document.getElementById('releaseId').value);f.append('file',x.file,x.name);q.timeout=25*60*1000;q.upload.onprogress=a=>{if(a.lengthComputable){out.innerHTML='<div class="notice"><b>Uploading '+(i+1)+' of '+total+': '+e(x.name)+'</b><div class="progress"><div id="bar" class="progress-bar"></div></div><div class="upload-meta">'+(a.loaded/a.total*100).toFixed(1)+'% · '+(a.loaded/1024/1024).toFixed(2)+' MB / '+(a.total/1024/1024).toFixed(2)+' MB</div></div>';document.getElementById('bar').style.width=(a.loaded/a.total*100)+'%'}};q.onload=()=>{let d;try{d=JSON.parse(q.responseText)}catch{d={ok:false,error:'Invalid response'}};resolve({ok:q.status>=200&&q.status<300&&d.ok,name:x.name,error:d.error})};q.onerror=()=>{if(tryNo<2){setTimeout(()=>resolve(uploadOne(x,i,total,k,tryNo+1)),1000);return}resolve({ok:false,name:x.name,error:'Connection interrupted after retries.'})};q.ontimeout=()=>resolve({ok:false,name:x.name,error:'Upload timed out.'});q.open('POST','/api/upload');q.setRequestHeader('x-admin-key',k);q.send(f)})}async function uploadAll(){const k=ki.value.trim();if(!k){out.innerHTML='<div class="notice">Admin key required.</div>';return}if(!files.length){out.innerHTML='<div class="notice">Select at least one file.</div>';return}localStorage.setItem('release_admin_key',k);kb.style.display='none';btn.disabled=true;btn.textContent='Uploading…';const r=[];for(let i=0;i<files.length;i++)r.push(await uploadOne(files[i],i,files.length,k));const bad=r.filter(x=>!x.ok);out.innerHTML='<div class="notice"><b>'+(!bad.length?'✓ All files uploaded successfully.':bad.length+' file(s) failed.')+'</b><br>'+r.map(x=>(x.ok?'✓ ':'✕ ')+e(x.name)+(x.ok?'':' — '+e(x.error))).join('<br>')+'</div>';btn.disabled=false;btn.textContent='Upload selected files ↑';if(!bad.length){fi.value='';files.length=0;render()}}btn.onclick=uploadAll;</script>`;s.send(page("Admin",body))}catch(e){s.status(500).send(page("Admin Error",`<div class="notice">${esc(e.message)}</div>`))}});

app.post("/api/upload",requireAdmin,upload.single("file"),async(req,res)=>{const temp=req.file?.path;try{if(!req.file)return res.status(400).json({ok:false,error:'No file was uploaded.'});if(!req.body.release_id)return res.status(400).json({ok:false,error:'release_id is required.'});const name=safeName(req.file.originalname);const result=await uploadAsset(req.body.release_id,name,req.file.mimetype,temp,req.file.size);if(!result.ok)return res.status(result.status).json({ok:false,error:result.data?.message||'GitHub upload failed.',github:result.data});res.json({ok:true,asset:result.data})}catch(e){res.status(502).json({ok:false,error:e.message})}finally{if(temp)fs.promises.unlink(temp).catch(()=>{})}});
app.patch('/api/assets/:id',requireAdmin,async(req,res)=>{try{const name=safeName(req.body?.name);if(!name)return res.status(400).json({ok:false,error:'A valid filename is required.'});const asset=await githubApi(`/repos/${REPO}/releases/assets/${encodeURIComponent(req.params.id)}`,{method:'PATCH',body:JSON.stringify({name})});res.json({ok:true,asset})}catch(e){res.status(e.status||500).json({ok:false,error:e.message})}});
app.delete('/api/assets/:id',requireAdmin,async(req,res)=>{try{await githubApi(`/repos/${REPO}/releases/assets/${encodeURIComponent(req.params.id)}`,{method:'DELETE'});res.json({ok:true})}catch(e){res.status(e.status||500).json({ok:false,error:e.message})}});
app.use((e,_q,res,_n)=>{if(e instanceof multer.MulterError)return res.status(e.code==='LIMIT_FILE_SIZE'?413:400).json({ok:false,error:e.code==='LIMIT_FILE_SIZE'?`File is too large. Maximum allowed size is ${MAX_UPLOAD_MB} MB.`:e.message});res.status(500).json({ok:false,error:e.message||'Internal server error.'})});
app.get('/health',(_q,res)=>res.type('text').send('ok'));app.listen(PORT,'0.0.0.0',()=>console.log(`${SITE_NAME} running on port ${PORT}`));
